#version 430
layout(location=0) in vec4 in_position;

uniform mat4 model_matrix;
uniform mat4 view_matrix;
uniform mat4 projectionMatrix;

void main(void){
    gl_Position = (projectionMatrix *view_matrix * model_matrix) * in_position;
}
